= form_for @fetcher do |f|
  - if @fetcher.errors.any?
    #error_explanation
      %h2= "#{pluralize(@fetcher.errors.count, "error")} prohibited this fetcher from being saved:"
      %ul
        - @fetcher.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
